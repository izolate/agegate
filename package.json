{
  "name": "agegate",
  "version": "1.0.0",
  "description": "Limit access to your app with an age gate",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/izolate/agegate.git"
  },
  "directories": {
    "example": "example"
  },
  "scripts": {
    "develop": "npm run watch",
    "watch": "watch 'npm run build' ./src",
    "build": "babel --modules common src/ --out-dir dist/",
    "example": "npm run jade && npm run js:example",
    "example:watch": "npm run jade && npm run watchify",
    "jade": "jade -P example/index.jade -o example/",
    "watchify": "watchify example/agegate.js -o example/agegate-compiled.js -v -t babelify",
    "js:test": "browserify test/agegate.js -o test/agegate-compiled.js",
    "js:example": "browserify example/agegate.js -t babelify -o example/agegate-compiled.js",
    "server:test": "http-server test"
  },
  "keywords": [
    "age",
    "gate",
    "agegate",
    "age gate",
    "adult",
    "verification"
  ],
  "author": "izolate <yoshtalwar@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.1.8",
    "babelify": "^6.0.2",
    "browserify": "^9.0.8",
    "http-server": "^0.8.0",
    "jade": "^1.9.2",
    "watch": "^0.16.0",
    "watchify": "^3.1.0"
  }
}
